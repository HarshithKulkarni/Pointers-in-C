#include<stdio.h>
int compute(int *a,int *b);
int comute(int *a,int *b)
{
    int sum;
    sum=*a + *b;
    printf("The sum is %d\n",sum);
    
}
int main()
{
    int a,b,sum;
    printf("Enter two numbers\n");
    scanf("%d%d",&a,&b);
    compute(&a,&b);
}
